{
  "name": "budget-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Budget Tracker — mono-user expense API",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node --env-file-if-exists=.env src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "setup": "node --env-file-if-exists=.env src/setup.js",
    "test": "node --test 'tests/**/*.test.js'"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "express": "^5.2.1",
    "express-openapi-validator": "^5.5.7"
  }
}
